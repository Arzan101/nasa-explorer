services:
  - type: web
    name: nasa-explorer-api
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NASA_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
